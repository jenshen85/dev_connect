const initialState = {
  isAuthenticated: false,
  user: {},
};
const authReduser = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default authReduser;
